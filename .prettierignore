# Agent-local: vendored skill packs and worktrees. Neither is ours to format,
# and a worktree checked out inside the repository would otherwise be linted
# twice.
.claude/
.agents/skills/

# Not ours to format.
node_modules/
dist/

# Generated by `scripts/registry-schemas.ts` on every typecheck. Formatting it
# by hand puts the two in a loop: prettier rewrites it, the next typecheck
# regenerates it, and `npm run check` leaves the tree dirty either way.
src/registry/schemas.generated.ts
