# Copy to .env.local (gitignored) and keep the value one of the credentials
# in src/auth/whitelist.ts. Loaded by `dev` and `mcp`; see D50 in docs/agents/rationale.json.
DASHBOARD_CREDENTIAL=dev-owner
