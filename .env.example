# Copy to .env.local (gitignored) and keep the value one of the credentials
# in src/auth/whitelist.ts. Loaded by `dev` and `mcp`; see docs/adr/0001-auth-delegated-to-swappable-provider.md.
DASHBOARD_CREDENTIAL=dev-owner
